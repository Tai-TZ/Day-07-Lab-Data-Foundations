# RAG Workflow Studio

Frontend tích hợp trong repo lab Day-07. Nguồn UI: [rag-reveal](https://github.com/NguyenTrongNguyen04/rag-reveal).

Chạy backend từ thư mục **gốc** lab (`uvicorn rag_api:app`), rồi:

```bash
npm install
copy .env.example .env
npm run dev
```
